# Página NPS — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar página `/nps` com 4 KPIs (envios, aberturas, respostas, média nota), tabela de envios detalhada e botão para baixar CSV dos clientes pendentes de resposta — respeitando escopo por role do usuário logado.

**Architecture:** Rota Hono nova em `server/src/routes/nps.ts` com 2 endpoints (`GET /nps` JSON e `GET /nps/pendentes.csv`), consultando `nps_envios` + `LEFT JOIN base_clientes`. Frontend em `src/app/(auth)/nps/page.tsx` como Server Component + `NpsTable.tsx` client. Filtro por role via `resolveFilterFromCtx` extraído para módulo compartilhado. Zero teste automatizado (segue padrão atual do projeto — validação manual).

**Tech Stack:** Next.js 15 App Router + React 19 + TS · Hono no Cloudflare Worker · D1 (SQLite edge) `nobel-performance-db` · Tailwind v4 tokens do design system Nobel · sem dependência nova.

**Spec:** [docs/superpowers/specs/2026-08-11-nps-page-design.md](../specs/2026-08-11-nps-page-design.md)

## Global Constraints

- **Sem dependência nova**: CSV gerado com string plain; nada de `xlsx`/`csv-writer`.
- **Filtro por role obrigatório em toda query**: admin/master = base inteira; lider = por equipe (join com `assessores`); lider_pj/assessor = por `id_assessor` do header.
- **Tokens do design system**: usar apenas variáveis CSS existentes (`var(--bg-elev)`, `var(--line)`, `var(--e-float)`, `var(--f-text)`, `var(--f-mono)`, `var(--fg)`, `var(--fg-faint)`, `var(--fg-mute)`, `var(--pos-bg)`, `var(--pos-fg)`, `var(--neg-bg)`, `var(--neg-fg)`, `var(--bg-deep)`, `var(--c-gold)`, `var(--r-pill)`, `var(--s-3)`, `var(--s-4)`). Nada hard-coded.
- **`nps_assessor > 0` na média**: nota zero é filtrada (padrão de `qualidade.ts`).
- **Fallback nome_cliente**: se cliente não existir em `base_clientes`, `'Cliente ' || id_cliente`.
- **CSV com BOM UTF-8**: primeiro byte `﻿` para Excel abrir corretamente.
- **Sem filtro temporal**: `nps_envios` já vem do mês corrente (fonte externa).
- **Commit por task**: usar Conventional Commits com scope `nps` (ex: `feat(nps): ...`).

## File Structure

```
server/
├── src/
│   ├── lib/
│   │   └── role-filter.ts        [NEW — Task 1] extraído de performance.ts
│   ├── routes/
│   │   ├── nps.ts                [NEW — Tasks 2, 3]
│   │   └── performance.ts        [MODIFY — Task 1: passa a importar de role-filter.ts]
│   └── index.ts                  [MODIFY — Task 2: monta /nps]

src/
├── app/
│   ├── (auth)/
│   │   ├── _components/
│   │   │   └── Sidebar.tsx       [MODIFY — Task 7: adiciona item NPS]
│   │   └── nps/
│   │       ├── page.tsx          [NEW — Task 5]
│   │       └── _components/
│   │           └── NpsTable.tsx  [NEW — Task 6]
│   └── api/
│       └── nps/
│           ├── route.ts                    [NEW — Task 4]
│           └── pendentes.csv/
│               └── route.ts                [NEW — Task 4]
└── lib/
    └── mock/
        └── demo.ts               [MODIFY — Task 7: adiciona demoNps]
```

---

## Task 1: Extrair helpers de role-filter para módulo compartilhado

Refactor puro. Move `FilterResult`, `resolveFilter`, `resolveFilterFromCtx`, `buildAndFilter`, `buildWhereFilter`, `buildDivFilter` e a constante `LIDER_PJ_ALL_PATHS` de `performance.ts` para um novo `server/src/lib/role-filter.ts`. `performance.ts` passa a importar. Zero mudança de comportamento — objetivo é permitir reuso em `nps.ts`.

**Files:**
- Create: `server/src/lib/role-filter.ts`
- Modify: `server/src/routes/performance.ts:8-109` (remover as declarações, adicionar import no topo)

**Interfaces:**
- Consumes: nada
- Produces:
  ```ts
  // server/src/lib/role-filter.ts
  export type FilterResult =
    | { type: 'all' }
    | { type: 'assessor'; id: string }
    | { type: 'equipe'; equipe: string }
    | { type: 'denied' }
  export type ResolveOpts = { scopeForLiderPj?: 'all' }
  export async function resolveFilter(
    db: D1Database, role: string | undefined, email: string | undefined,
    equipe: string | undefined, idAssessor: string | undefined,
    filterType?: string, filterValue?: string, opts?: ResolveOpts,
  ): Promise<FilterResult>
  export async function resolveFilterFromCtx(
    c: Context<{ Bindings: Env; Variables: Variables }>,
    opts?: ResolveOpts,
  ): Promise<FilterResult>
  export function buildWhereFilter(f: FilterResult, col?: string): string
  export function buildAndFilter(f: FilterResult, col?: string): string
  export function buildDivFilter(f: FilterResult): string
  export const LIDER_PJ_ALL_PATHS: string[]
  ```

- [ ] **Step 1: Criar `server/src/lib/role-filter.ts`**

Copiar o bloco inteiro das linhas 8-109 de `server/src/routes/performance.ts` para o novo arquivo, prefixar cada declaração top-level com `export`, e ajustar imports no topo:

```ts
// server/src/lib/role-filter.ts
import type { Context } from 'hono'
import type { Env, Variables } from '../types'

/* ─── Filtro por role ────────────────────────────────────────────────────────
   admin/master → acesso total (sem filtro)
   lider        → filtro por equipe (subquery em assessores)
   lider_pj     → líder sem equipe. Default: filtra pelo id_assessor da session.
                  Rotas de Carteiras/Clientes chamam com { scopeForLiderPj: 'all' }
                  para forçar visão geral.
   assessor     → filtro por id_assessor (lookup por email)
   denied       → acesso negado
 ─────────────────────────────────────────────────────────────────────────── */
export type FilterResult =
  | { type: 'all' }
  | { type: 'assessor'; id: string }
  | { type: 'equipe'; equipe: string }
  | { type: 'denied' }

export type ResolveOpts = { scopeForLiderPj?: 'all' }

export async function resolveFilter(
  db: D1Database,
  role: string | undefined,
  email: string | undefined,
  equipe: string | undefined,
  idAssessor: string | undefined,
  filterType?: string | undefined,
  filterValue?: string | undefined,
  opts?: ResolveOpts,
): Promise<FilterResult> {
  if (role === 'admin' || role === 'master') {
    if (filterType === 'equipe' && filterValue) return { type: 'equipe', equipe: filterValue }
    if (filterType === 'assessor' && filterValue) return { type: 'assessor', id: filterValue }
    return { type: 'all' }
  }
  if (role === 'lider') {
    if (!equipe) return { type: 'denied' }
    if (filterType === 'assessor' && filterValue) return { type: 'assessor', id: filterValue }
    return { type: 'equipe', equipe }
  }
  if (role === 'lider_pj') {
    if (opts?.scopeForLiderPj === 'all') {
      if (filterType === 'equipe' && filterValue) return { type: 'equipe', equipe: filterValue }
      if (filterType === 'assessor' && filterValue) return { type: 'assessor', id: filterValue }
      return { type: 'all' }
    }
    if (!idAssessor) return { type: 'denied' }
    return { type: 'assessor', id: idAssessor }
  }
  if (!email) return { type: 'denied' }
  const row = await db
    .prepare('SELECT id_assessor FROM assessores WHERE LOWER(mail_assessor) = LOWER(?)')
    .bind(email)
    .first<{ id_assessor: string }>()
  return row ? { type: 'assessor', id: row.id_assessor } : { type: 'denied' }
}

/** Rotas em que `lider_pj` vê a base inteira (visão geral). */
export const LIDER_PJ_ALL_PATHS = ['/performance/carteiras', '/performance/clientes', '/performance/pj1']

/** Boilerplate helper — lê os headers X-User-* do contexto e chama resolveFilter. */
export async function resolveFilterFromCtx(
  c: Context<{ Bindings: Env; Variables: Variables }>,
  opts?: ResolveOpts,
): Promise<FilterResult> {
  const path = c.req.path
  const inferredScope = LIDER_PJ_ALL_PATHS.some((p) => path === p || path.startsWith(`${p}/`))
    ? 'all'
    : undefined
  return resolveFilter(
    c.env.PERF_DB,
    c.req.header('X-User-Role'),
    c.req.header('X-User-Email'),
    c.req.header('X-User-Equipe'),
    c.req.header('X-User-Id-Assessor'),
    c.req.header('X-Filter-Type'),
    c.req.header('X-Filter-Value'),
    { scopeForLiderPj: opts?.scopeForLiderPj ?? inferredScope },
  )
}

export function buildWhereFilter(f: FilterResult, col = 'id_assessor'): string {
  if (f.type === 'all')      return ''
  if (f.type === 'assessor') return ` WHERE ${col} = '${f.id}'`
  if (f.type === 'equipe')   return ` WHERE ${col} IN (SELECT id_assessor FROM assessores WHERE equipe = '${f.equipe}')`
  return ''
}

export function buildAndFilter(f: FilterResult, col = 'id_assessor'): string {
  if (f.type === 'all')      return ''
  if (f.type === 'assessor') return ` AND ${col} = '${f.id}'`
  if (f.type === 'equipe')   return ` AND ${col} IN (SELECT id_assessor FROM assessores WHERE equipe = '${f.equipe}')`
  return ''
}

export function buildDivFilter(f: FilterResult): string {
  if (f.type === 'all')      return ''
  if (f.type === 'assessor') return ` WHERE id_cliente IN (SELECT id_cliente FROM tb_positivador WHERE id_assessor = '${f.id}')`
  if (f.type === 'equipe')   return ` WHERE id_cliente IN (SELECT id_cliente FROM tb_positivador WHERE id_assessor IN (SELECT id_assessor FROM assessores WHERE equipe = '${f.equipe}'))`
  return ''
}
```

- [ ] **Step 2: Atualizar `server/src/routes/performance.ts` para importar do novo módulo**

Remover as linhas 8-109 (todo o bloco de tipos + funções + `LIDER_PJ_ALL_PATHS`) e substituir por:

```ts
import {
  resolveFilterFromCtx,
  buildAndFilter,
  buildWhereFilter,
  buildDivFilter,
  type FilterResult,
} from '../lib/role-filter'
```

Também remover o import não usado `import { Hono, type Context } from 'hono'` → passa a `import { Hono } from 'hono'` (Context sai — só era usado no helper que foi movido). Verificar que os usos restantes de `FilterResult` na tipagem ainda funcionam.

- [ ] **Step 3: Validar typecheck**

Run: `npm run typecheck`
Expected: PASS sem erros novos.

- [ ] **Step 4: Validar que o worker ainda sobe**

Run: `cd server && npm run dev`
Expected: worker inicia sem erro, endpoint `curl http://localhost:8787/health` responde `{"status":"ok",...}`.

Parar o worker (`Ctrl+C`).

- [ ] **Step 5: Commit**

```bash
git add server/src/lib/role-filter.ts server/src/routes/performance.ts
git commit -m "refactor(api): extrai helpers de role-filter para módulo compartilhado

Prepara reuso do resolveFilterFromCtx/buildAndFilter/buildWhereFilter
na nova rota /nps. Zero mudança de comportamento em /performance."
```

---

## Task 2: Backend — endpoint `GET /nps` (KPIs + envios em JSON)

Criar `server/src/routes/nps.ts` com o endpoint que retorna `{ kpis, envios }` filtrado por role, e montá-lo em `server/src/index.ts`.

**Files:**
- Create: `server/src/routes/nps.ts`
- Modify: `server/src/index.ts:11` (adicionar import), `server/src/index.ts:48` (montar rota)

**Interfaces:**
- Consumes: `resolveFilterFromCtx`, `buildAndFilter` de `../lib/role-filter` (Task 1)
- Produces:
  ```ts
  // GET /nps → 200
  {
    data: {
      kpis: {
        envios: number
        aberturas: number
        respostas: number
        mediaNota: number | null
      }
      envios: Array<{
        idCliente: number
        nomeCliente: string
        emailCliente: string | null
        telefone: string | null
        nomeAssessor: string | null
        recordDate: string
        emailOpened: string | null    // 'Sim' | 'Não' | null
        surveyFinished: string | null // 'Sim' | 'Não' | null
        nota: number | null
      }>
    }
  }
  ```

- [ ] **Step 1: Criar `server/src/routes/nps.ts`**

```ts
import { Hono } from 'hono'
import type { Env, Variables } from '../types'
import { resolveFilterFromCtx, buildAndFilter } from '../lib/role-filter'

const app = new Hono<{ Bindings: Env; Variables: Variables }>()

// Auth: internal secret (Next.js SSR) OU Cf-Access-Jwt-Assertion (browser)
app.use('*', async (c, next) => {
  const auth = c.req.header('Authorization') ?? ''
  const secret = c.env.INTERNAL_API_SECRET ?? 'dev-perf-secret-2026'
  if (auth === `Bearer ${secret}`) return next()
  if (c.req.header('Cf-Access-Jwt-Assertion')) return next()
  return c.json({ error: 'Unauthorized' }, 401)
})

/* ── GET /nps ──────────────────────────────────────────────────────────────
   KPIs + linhas da tabela em uma única round-trip.
   Base sempre no mês corrente (dado externo — sem filtro temporal).
──────────────────────────────────────────────────────────────────────────── */
app.get('/', async (c) => {
  const db = c.env.PERF_DB

  const filter = await resolveFilterFromCtx(c)
  if (filter.type === 'denied') return c.json({ error: 'Forbidden' }, 403)

  // O filtro se aplica a ne.id_assessor
  const wf = buildAndFilter(filter, 'ne.id_assessor')

  const [kpisRow, enviosRows] = await Promise.all([
    db
      .prepare(`
        SELECT
          SUM(CASE WHEN email_sent      = 'Sim' THEN 1 ELSE 0 END) AS envios,
          SUM(CASE WHEN email_opened    = 'Sim' THEN 1 ELSE 0 END) AS aberturas,
          SUM(CASE WHEN survey_finished = 'Sim' THEN 1 ELSE 0 END) AS respostas,
          ROUND(
            AVG(CASE WHEN survey_finished = 'Sim' AND nps_assessor > 0 THEN nps_assessor END),
            2
          ) AS media_nota
        FROM nps_envios ne
        WHERE 1=1${wf}
      `)
      .first<{
        envios: number | null
        aberturas: number | null
        respostas: number | null
        media_nota: number | null
      }>(),
    db
      .prepare(`
        SELECT
          ne.id_cliente                                                 AS id_cliente,
          COALESCE(bc.nome_cliente, 'Cliente ' || ne.id_cliente)        AS nome_cliente,
          bc.email_cliente                                              AS email_cliente,
          bc.telefone                                                   AS telefone,
          bc.nome_assessor                                              AS nome_assessor,
          ne.record_date                                                AS record_date,
          ne.email_opened                                               AS email_opened,
          ne.survey_finished                                            AS survey_finished,
          ne.nps_assessor                                               AS nota
        FROM nps_envios ne
        LEFT JOIN base_clientes bc ON bc.id_cliente = ne.id_cliente
        WHERE 1=1${wf}
        ORDER BY ne.record_date DESC
      `)
      .all<{
        id_cliente: number
        nome_cliente: string
        email_cliente: string | null
        telefone: string | null
        nome_assessor: string | null
        record_date: string
        email_opened: string | null
        survey_finished: string | null
        nota: number | null
      }>(),
  ])

  return c.json({
    data: {
      kpis: {
        envios: kpisRow?.envios ?? 0,
        aberturas: kpisRow?.aberturas ?? 0,
        respostas: kpisRow?.respostas ?? 0,
        mediaNota: kpisRow?.media_nota ?? null,
      },
      envios: (enviosRows.results ?? []).map((r) => ({
        idCliente: r.id_cliente,
        nomeCliente: r.nome_cliente,
        emailCliente: r.email_cliente,
        telefone: r.telefone,
        nomeAssessor: r.nome_assessor,
        recordDate: r.record_date,
        emailOpened: r.email_opened,
        surveyFinished: r.survey_finished,
        nota: r.nota,
      })),
    },
  })
})

export default app
```

- [ ] **Step 2: Montar rota em `server/src/index.ts`**

No topo, na seção de imports (por volta da linha 11-12), adicionar:

```ts
import npsRouter from './routes/nps'
```

Na seção de rotas (por volta da linha 48, após `app.route('/auth', authRouter)`), adicionar:

```ts
app.route('/nps', npsRouter)
```

- [ ] **Step 3: Subir o worker local**

Run: `cd server && npm run dev`
Expected: worker inicia, log de startup mostra binding `PERF_DB`.

Manter rodando para próximo step.

- [ ] **Step 4: Validar endpoint como admin (curl)**

Run em outro terminal:
```bash
curl -s -H "Authorization: Bearer dev-perf-secret-2026" \
     -H "X-User-Role: admin" \
     http://localhost:8787/nps | jq '.data.kpis'
```
Expected:
```json
{
  "envios": 744,
  "aberturas": 311,
  "respostas": 26,
  "mediaNota": 9.14
}
```

(Números conferidos em 2026-08-11. Se a base atualizou, apenas verifique que a estrutura bate — `envios >= aberturas >= respostas` e `mediaNota` entre 0-10.)

- [ ] **Step 5: Validar filtro por role — assessor**

Precisamos de um `id_assessor` que tenha envios. Descubra com:
```bash
curl -s -H "Authorization: Bearer dev-perf-secret-2026" \
     -H "X-User-Role: admin" \
     http://localhost:8787/nps | jq '.data.envios[0]'
```

Pegue o `nomeAssessor` da primeira linha, procure em `assessores` o `id_assessor` correspondente (ou pegue via query direta em D1 se preferir). Depois:

```bash
curl -s -H "Authorization: Bearer dev-perf-secret-2026" \
     -H "X-User-Role: assessor" \
     -H "X-User-Email: qualquer@teste.com" \
     -H "X-User-Id-Assessor: <ID>" \
     http://localhost:8787/nps | jq '.data.kpis, (.data.envios | length)'
```
Expected: números **menores** que o total de admin. `envios` recuperados devem todos ter `id_assessor` = `<ID>` (verificar amostrando `.data.envios[0]`).

- [ ] **Step 6: Validar 401 sem auth**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8787/nps
```
Expected: `401`.

Parar o worker (`Ctrl+C`).

- [ ] **Step 7: Commit**

```bash
git add server/src/routes/nps.ts server/src/index.ts
git commit -m "feat(nps): endpoint /nps com KPIs e envios filtrados por role

Fonte: nps_envios + LEFT JOIN base_clientes. resolveFilterFromCtx
aplica o filtro por role (admin/master=all, lider=equipe,
assessor=id_assessor)."
```

---

## Task 3: Backend — endpoint `GET /nps/pendentes.csv`

Adicionar o endpoint que retorna os clientes que receberam email mas não responderam, em CSV com BOM UTF-8, respeitando o filtro por role.

**Files:**
- Modify: `server/src/routes/nps.ts` (adicionar o segundo endpoint antes de `export default app`)

**Interfaces:**
- Consumes: `resolveFilterFromCtx`, `buildAndFilter` (já importados na Task 2)
- Produces:
  ```
  GET /nps/pendentes.csv → 200
  Content-Type: text/csv; charset=utf-8
  Content-Disposition: attachment; filename="nps-pendentes-YYYY-MM-DD.csv"
  Body: BOM + "nome_cliente,email_cliente,telefone,nome_assessor,record_date\n..."
  ```

- [ ] **Step 1: Adicionar helper de escape no topo de `nps.ts`**

Logo após o `const app = new Hono...`, adicionar:

```ts
/** Escapa um campo CSV: se contém vírgula, aspas ou newline, envolve em aspas
 *  e duplica aspas internas. */
function csvField(v: string | number | null | undefined): string {
  if (v === null || v === undefined) return ''
  const s = String(v)
  if (/[",\n\r]/.test(s)) return `"${s.replace(/"/g, '""')}"`
  return s
}
```

- [ ] **Step 2: Adicionar o endpoint `/pendentes.csv`**

Antes de `export default app`, adicionar:

```ts
/* ── GET /nps/pendentes.csv ────────────────────────────────────────────────
   CSV dos clientes que receberam email mas ainda não responderam.
   Respeita o filtro por role (assessor baixa só os pendentes dele).
──────────────────────────────────────────────────────────────────────────── */
app.get('/pendentes.csv', async (c) => {
  const db = c.env.PERF_DB

  const filter = await resolveFilterFromCtx(c)
  if (filter.type === 'denied') return c.json({ error: 'Forbidden' }, 403)

  const wf = buildAndFilter(filter, 'ne.id_assessor')

  const rows = await db
    .prepare(`
      SELECT
        COALESCE(bc.nome_cliente, 'Cliente ' || ne.id_cliente) AS nome_cliente,
        bc.email_cliente                                       AS email_cliente,
        bc.telefone                                            AS telefone,
        bc.nome_assessor                                       AS nome_assessor,
        ne.record_date                                         AS record_date
      FROM nps_envios ne
      LEFT JOIN base_clientes bc ON bc.id_cliente = ne.id_cliente
      WHERE ne.email_sent = 'Sim'
        AND (ne.survey_finished IS NULL OR ne.survey_finished != 'Sim')
        ${wf}
      ORDER BY ne.record_date DESC
    `)
    .all<{
      nome_cliente: string
      email_cliente: string | null
      telefone: string | null
      nome_assessor: string | null
      record_date: string
    }>()

  const header = 'nome_cliente,email_cliente,telefone,nome_assessor,record_date'
  const body = (rows.results ?? [])
    .map((r) =>
      [
        csvField(r.nome_cliente),
        csvField(r.email_cliente),
        csvField(r.telefone),
        csvField(r.nome_assessor),
        csvField(r.record_date),
      ].join(','),
    )
    .join('\n')

  const today = new Date().toISOString().slice(0, 10)
  const csv = `﻿${header}\n${body}\n`

  return new Response(csv, {
    status: 200,
    headers: {
      'Content-Type': 'text/csv; charset=utf-8',
      'Content-Disposition': `attachment; filename="nps-pendentes-${today}.csv"`,
    },
  })
})
```

- [ ] **Step 3: Subir o worker local**

Run: `cd server && npm run dev`

- [ ] **Step 4: Validar CSV como admin**

Run:
```bash
curl -s -H "Authorization: Bearer dev-perf-secret-2026" \
     -H "X-User-Role: admin" \
     http://localhost:8787/nps/pendentes.csv | head -5
```
Expected: primeira linha é o header (`nome_cliente,email_cliente,...`) precedida do BOM invisível (é ok se `head` mostrar `?nome_cliente...` — o `?` é o BOM). Devem aparecer 4+ linhas de dados. Todas as linhas devem ter `survey_finished != 'Sim'`.

Contar linhas de dados:
```bash
curl -s -H "Authorization: Bearer dev-perf-secret-2026" \
     -H "X-User-Role: admin" \
     http://localhost:8787/nps/pendentes.csv | wc -l
```
Expected: **>= 700** (pendentes = envios - respostas ≈ 744 - 26 = 718, mais header/newline).

- [ ] **Step 5: Validar headers do CSV**

Run:
```bash
curl -s -I -H "Authorization: Bearer dev-perf-secret-2026" \
     -H "X-User-Role: admin" \
     http://localhost:8787/nps/pendentes.csv
```
Expected: `Content-Type: text/csv; charset=utf-8` e `Content-Disposition: attachment; filename="nps-pendentes-YYYY-MM-DD.csv"`.

Parar o worker.

- [ ] **Step 6: Commit**

```bash
git add server/src/routes/nps.ts
git commit -m "feat(nps): endpoint /nps/pendentes.csv com BOM UTF-8

Baixa clientes que receberam email mas não responderam, respeitando
role. BOM + text/csv para Excel abrir sem quebrar acentos."
```

---

## Task 4: Frontend — proxies Next.js para os dois endpoints

Criar os dois arquivos de proxy que a página vai chamar. Padrão idêntico ao de `src/app/api/performance/carteiras/route.ts:1-18`.

**Files:**
- Create: `src/app/api/nps/route.ts`
- Create: `src/app/api/nps/pendentes.csv/route.ts`

**Interfaces:**
- Consumes: `apiFetch` de `@/lib/api/fetch`, `getSession` de `@/lib/auth/session`
- Produces:
  - `GET /api/nps` → mesmo payload do Worker `GET /nps`
  - `GET /api/nps/pendentes.csv` → mesmo Response CSV do Worker, para uso em `<a href="/api/nps/pendentes.csv" download>`

- [ ] **Step 1: Criar `src/app/api/nps/route.ts`**

```ts
import { apiFetch } from '@/lib/api/fetch'
import { getSession } from '@/lib/auth/session'
import { NextResponse } from 'next/server'

export async function GET() {
  const session = await getSession()
  if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const res = await apiFetch('/nps', {
    headers: {
      'X-User-Email': session.email,
      'X-User-Role': session.role,
      'X-User-Equipe': session.equipe ?? '',
      ...(session.idAssessor ? { 'X-User-Id-Assessor': session.idAssessor } : {}),
    },
  })

  const json = await res.json()
  return NextResponse.json(json, { status: res.status })
}
```

- [ ] **Step 2: Criar `src/app/api/nps/pendentes.csv/route.ts`**

O proxy do CSV **não pode** usar `NextResponse.json` — precisa repassar o body como stream e preservar os headers `Content-Type`/`Content-Disposition`:

```ts
import { apiFetch } from '@/lib/api/fetch'
import { getSession } from '@/lib/auth/session'
import { NextResponse } from 'next/server'

export async function GET() {
  const session = await getSession()
  if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const res = await apiFetch('/nps/pendentes.csv', {
    headers: {
      'X-User-Email': session.email,
      'X-User-Role': session.role,
      'X-User-Equipe': session.equipe ?? '',
      ...(session.idAssessor ? { 'X-User-Id-Assessor': session.idAssessor } : {}),
    },
  })

  if (!res.ok) {
    return NextResponse.json({ error: 'Falha ao gerar CSV' }, { status: res.status })
  }

  const body = await res.arrayBuffer()
  return new NextResponse(body, {
    status: 200,
    headers: {
      'Content-Type': res.headers.get('content-type') ?? 'text/csv; charset=utf-8',
      'Content-Disposition':
        res.headers.get('content-disposition') ??
        `attachment; filename="nps-pendentes-${new Date().toISOString().slice(0, 10)}.csv"`,
    },
  })
}
```

- [ ] **Step 3: Validar typecheck**

Run: `npm run typecheck`
Expected: PASS.

- [ ] **Step 4: Rodar dev server e testar proxy JSON**

Run em terminais separados:
- `cd server && npm run dev` (worker local)
- `npm run dev` (Next.js local)

Depois de logar em `http://localhost:3000` (qualquer conta), abra `http://localhost:3000/api/nps` — deve baixar/mostrar o mesmo JSON do Worker.

- [ ] **Step 5: Testar proxy CSV**

Abrir `http://localhost:3000/api/nps/pendentes.csv` no browser — deve iniciar download de `nps-pendentes-YYYY-MM-DD.csv`. Abrir no Excel/LibreOffice, conferir que:
- Acentos em `nome_cliente` aparecem corretamente (BOM funcionando).
- Cabeçalho: `nome_cliente,email_cliente,telefone,nome_assessor,record_date`.

Parar os dev servers.

- [ ] **Step 6: Commit**

```bash
git add src/app/api/nps/route.ts src/app/api/nps/pendentes.csv/route.ts
git commit -m "feat(nps): proxies Next.js para /api/nps e /api/nps/pendentes.csv

Repassa headers de session (X-User-*) para o Worker. Proxy CSV
preserva Content-Type e Content-Disposition."
```

---

## Task 5: Frontend — página `/nps` (Server Component com KPIs + botão)

Criar a página que renderiza os 4 KPI cards e o botão CSV. Ainda sem tabela — vem na Task 6. Segue exatamente o padrão de `src/app/(auth)/clientes/page.tsx`.

**Files:**
- Create: `src/app/(auth)/nps/page.tsx`

**Interfaces:**
- Consumes: `apiFetch` de `@/lib/api/fetch`, `requireSession` de `@/lib/auth/session`, `PageGreeting` de `../_components/PageGreeting`. `NpsTable` **ainda não existe** — será importada em Task 6.
- Produces:
  ```ts
  // Payload esperado do /api/nps
  export type Envio = {
    idCliente: number
    nomeCliente: string
    emailCliente: string | null
    telefone: string | null
    nomeAssessor: string | null
    recordDate: string
    emailOpened: string | null
    surveyFinished: string | null
    nota: number | null
  }
  export type NpsPayload = {
    kpis: { envios: number; aberturas: number; respostas: number; mediaNota: number | null }
    envios: Envio[]
  }
  ```

- [ ] **Step 1: Criar `src/app/(auth)/nps/page.tsx`**

```tsx
import { PageGreeting } from '../_components/PageGreeting'
import { apiFetch } from '@/lib/api/fetch'
import { requireSession } from '@/lib/auth/session'

/* ─── Tipos ──────────────────────────────────────────────────────────────── */

export type Envio = {
  idCliente: number
  nomeCliente: string
  emailCliente: string | null
  telefone: string | null
  nomeAssessor: string | null
  recordDate: string
  emailOpened: string | null
  surveyFinished: string | null
  nota: number | null
}

type NpsPayload = {
  kpis: { envios: number; aberturas: number; respostas: number; mediaNota: number | null }
  envios: Envio[]
}

/* ─── Formatters ─────────────────────────────────────────────────────────── */

function fInt(n: number): string {
  return n.toLocaleString('pt-BR')
}

function fPct(a: number, b: number): string {
  if (b === 0) return '—'
  return `${Math.round((a / b) * 100)}%`
}

function fNota(n: number | null): string {
  if (n === null || n === undefined) return '—'
  return n.toFixed(2).replace('.', ',')
}

/* ─── Fetch ──────────────────────────────────────────────────────────────── */

async function getNps(
  email: string,
  role: string,
  equipe?: string,
  idAssessor?: string,
): Promise<NpsPayload | null> {
  try {
    const res = await apiFetch('/nps', {
      cache: 'no-store',
      headers: {
        'X-User-Email': email,
        'X-User-Role': role,
        'X-User-Equipe': equipe ?? '',
        ...(idAssessor ? { 'X-User-Id-Assessor': idAssessor } : {}),
      },
    })
    if (!res.ok) throw new Error(`HTTP ${res.status}`)
    const json = (await res.json()) as { data: NpsPayload }
    return json.data
  } catch {
    return null
  }
}

/* ─── Page ───────────────────────────────────────────────────────────────── */

export default async function NpsPage() {
  const session = await requireSession()
  const data = await getNps(session.email, session.role, session.equipe, session.idAssessor)

  const kpis = data?.kpis ?? { envios: 0, aberturas: 0, respostas: 0, mediaNota: null }

  /* ─── Estilos — padrão canônico de card (copiado de clientes/page.tsx) ── */
  const card: React.CSSProperties = {
    background: 'var(--bg-elev)',
    border: '1px solid var(--line)',
    borderRadius: 12,
    boxShadow: 'var(--e-float)',
    overflow: 'hidden',
  }
  const kpiLabel: React.CSSProperties = {
    fontFamily: 'var(--f-mono)', fontSize: 11, fontWeight: 500,
    color: 'var(--fg-faint)', textTransform: 'uppercase',
    letterSpacing: '.10em', marginBottom: 14,
  }
  const kpiValue: React.CSSProperties = {
    fontFamily: 'var(--f-text)', fontSize: 34, fontWeight: 700,
    color: 'var(--fg)', lineHeight: 1,
    letterSpacing: '-.02em', marginBottom: 12,
  }
  const kpiPill: React.CSSProperties = {
    display: 'inline-flex', alignItems: 'center',
    fontFamily: 'var(--f-mono)', fontSize: 11, fontWeight: 500,
    padding: '3px 10px', borderRadius: 'var(--r-pill)',
    background: 'var(--bg-deep)', color: 'var(--fg-mute)',
  }

  return (
    <div style={{ maxWidth: 1400 }}>
      <PageGreeting name={session.name} label="Pesquisa NPS" />

      {/* ── KPI Cards ─────────────────────────────────────────────────── */}
      <div
        style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(4, 1fr)',
          gap: 'var(--s-3)',
          marginBottom: 'var(--s-4)',
        }}
      >
        {/* Envios (com accent bar dourada) */}
        <div style={{ ...card, position: 'relative' }}>
          <div
            style={{
              position: 'absolute', top: 0, left: 0, right: 0, height: 2,
              background: 'linear-gradient(90deg, var(--c-gold), #D4AF6A)',
            }}
          />
          <div style={{ padding: '22px 24px' }}>
            <p style={kpiLabel}>Envios</p>
            <p style={kpiValue}>{fInt(kpis.envios)}</p>
            <span style={kpiPill}>base completa</span>
          </div>
        </div>

        {/* Aberturas */}
        <div style={card}>
          <div style={{ padding: '22px 24px' }}>
            <p style={kpiLabel}>Aberturas</p>
            <p style={kpiValue}>{fInt(kpis.aberturas)}</p>
            <span style={{ ...kpiPill, background: 'var(--pos-bg)', color: 'var(--pos-fg)' }}>
              {fPct(kpis.aberturas, kpis.envios)} dos envios
            </span>
          </div>
        </div>

        {/* Respostas */}
        <div style={card}>
          <div style={{ padding: '22px 24px' }}>
            <p style={kpiLabel}>Respostas</p>
            <p style={kpiValue}>{fInt(kpis.respostas)}</p>
            <span style={{ ...kpiPill, background: 'var(--pos-bg)', color: 'var(--pos-fg)' }}>
              {fPct(kpis.respostas, kpis.envios)} dos envios
            </span>
          </div>
        </div>

        {/* Média Nota */}
        <div style={card}>
          <div style={{ padding: '22px 24px' }}>
            <p style={kpiLabel}>Média Nota</p>
            <p style={kpiValue}>{fNota(kpis.mediaNota)}</p>
            <span style={kpiPill}>
              sobre {fInt(kpis.respostas)} {kpis.respostas === 1 ? 'resposta' : 'respostas'}
            </span>
          </div>
        </div>
      </div>

      {/* ── Botão CSV ─────────────────────────────────────────────────── */}
      <div style={{ marginBottom: 'var(--s-4)' }}>
        <a
          href="/api/nps/pendentes.csv"
          download
          style={{
            display: 'inline-flex', alignItems: 'center', gap: 8,
            padding: '10px 16px', borderRadius: 10,
            border: '1px solid var(--line-strong)',
            background: 'var(--bg-elev)',
            color: 'var(--fg)', fontFamily: 'var(--f-mono)',
            fontSize: 12, textDecoration: 'none',
          }}
        >
          ↓ Baixar clientes pendentes (CSV)
        </a>
      </div>

      {/* ── Tabela — placeholder até Task 6 ───────────────────────────── */}
      {data === null && (
        <div style={{ padding: 24, color: 'var(--fg-faint)', fontFamily: 'var(--f-text)' }}>
          Não foi possível carregar dados de NPS.
        </div>
      )}
      {data && data.envios.length === 0 && (
        <div style={{ padding: 24, color: 'var(--fg-faint)', fontFamily: 'var(--f-text)' }}>
          Sem envios de NPS no período.
        </div>
      )}
      {data && data.envios.length > 0 && (
        <div style={{ padding: 24, color: 'var(--fg-mute)', fontFamily: 'var(--f-text)' }}>
          {data.envios.length} envios carregados (tabela vem na Task 6).
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Validar typecheck**

Run: `npm run typecheck`
Expected: PASS.

- [ ] **Step 3: Rodar dev servers e abrir a página**

Run em terminais separados:
- `cd server && npm run dev`
- `npm run dev`

Abrir `http://localhost:3000/nps` (talvez precise digitar direto na URL — sidebar ainda não tem o link).

Conferir visualmente:
- 4 KPI cards renderizados na grid 4×1, gap uniforme.
- Card "Envios" tem a accent bar dourada no topo (2px, gradiente).
- Valores batem com o Worker (envios=744, aberturas=311, respostas=26, média=9,14 no scope admin).
- Card "Aberturas" e "Respostas" com pill verde (pos-bg/pos-fg) mostrando `X% dos envios`.
- Botão CSV renderiza com borda, ícone `↓` e texto em mono.
- Placeholder "744 envios carregados (tabela vem na Task 6)" aparece embaixo.

- [ ] **Step 4: Testar botão CSV**

Clicar no botão — deve baixar `nps-pendentes-YYYY-MM-DD.csv`. Abrir no Excel: acentos OK, ~718 linhas de dados.

- [ ] **Step 5: Testar em modo demo (opcional agora — será finalizado em Task 7)**

Se logar como usuário demo, a página vai mostrar "Não foi possível carregar dados de NPS." — comportamento esperado nesta task (mock vem em Task 7). Anotar mentalmente para validar depois.

Parar dev servers.

- [ ] **Step 6: Commit**

```bash
git add src/app/\(auth\)/nps/page.tsx
git commit -m "feat(nps): página /nps com 4 KPIs e botão CSV

Server Component seguindo padrão de clientes/page.tsx. Tabela e
demo mock vêm nas próximas tasks."
```

---

## Task 6: Frontend — `NpsTable` (Client Component com paginação)

Criar o Client Component da tabela e integrá-lo à página. Segue estilo de tabelas do projeto — border `var(--line)`, sem shadow, badges filled.

**Files:**
- Create: `src/app/(auth)/nps/_components/NpsTable.tsx`
- Modify: `src/app/(auth)/nps/page.tsx` (importar `NpsTable`, remover placeholder da Task 5)

**Interfaces:**
- Consumes: `Envio` type de `../page` (Task 5)
- Produces:
  ```ts
  export function NpsTable({ envios }: { envios: Envio[] }): JSX.Element
  ```

- [ ] **Step 1: Criar `src/app/(auth)/nps/_components/NpsTable.tsx`**

```tsx
'use client'

import { useState } from 'react'
import type { Envio } from '../page'

const PAGE_SIZE = 100

function fDate(iso: string): string {
  // record_date vem como "2026-07-15T10:32:00.000000"
  const d = new Date(iso.slice(0, 19))
  if (Number.isNaN(d.getTime())) return iso
  const dd = String(d.getUTCDate()).padStart(2, '0')
  const mm = String(d.getUTCMonth() + 1).padStart(2, '0')
  return `${dd}/${mm}`
}

function fNota(n: number | null): string {
  if (n === null || n === undefined) return '—'
  return n.toFixed(1).replace('.', ',')
}

function Badge({ ok }: { ok: boolean }) {
  const style: React.CSSProperties = {
    display: 'inline-block',
    padding: '2px 8px',
    borderRadius: 'var(--r-pill)',
    fontFamily: 'var(--f-mono)',
    fontSize: 10,
    fontWeight: 500,
    background: ok ? 'var(--pos-bg)' : 'var(--neg-bg)',
    color:      ok ? 'var(--pos-fg)' : 'var(--neg-fg)',
    borderColor: 'transparent',
  }
  return <span style={style}>{ok ? 'Sim' : 'Não'}</span>
}

export function NpsTable({ envios }: { envios: Envio[] }) {
  const [page, setPage] = useState(0)
  const totalPages = Math.max(1, Math.ceil(envios.length / PAGE_SIZE))
  const rows = envios.slice(page * PAGE_SIZE, (page + 1) * PAGE_SIZE)

  const wrapper: React.CSSProperties = {
    background: 'var(--bg-elev)',
    border: '1px solid var(--line)',
    borderRadius: 12,
    overflow: 'hidden',
  }
  const th: React.CSSProperties = {
    textAlign: 'left',
    padding: '10px 16px',
    fontFamily: 'var(--f-mono)',
    fontSize: 11,
    fontWeight: 500,
    color: 'var(--fg-faint)',
    textTransform: 'uppercase',
    letterSpacing: '.10em',
    borderBottom: '1px solid var(--line)',
    background: 'var(--bg-deep)',
  }
  const td: React.CSSProperties = {
    padding: '12px 16px',
    fontFamily: 'var(--f-text)',
    fontSize: 14,
    color: 'var(--fg)',
    borderBottom: '1px solid var(--line)',
    verticalAlign: 'top',
  }
  const idMono: React.CSSProperties = {
    fontFamily: 'var(--f-mono)',
    fontSize: 10,
    color: 'var(--fg-faint)',
    marginTop: 2,
  }
  const notaCell: React.CSSProperties = {
    ...td,
    fontFamily: 'var(--f-mono)',
    fontWeight: 500,
    textAlign: 'right',
    fontFeatureSettings: '"tnum"',
  }

  return (
    <div>
      <div style={wrapper}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            <tr>
              <th style={th}>Cliente</th>
              <th style={th}>Assessor</th>
              <th style={th}>Envio</th>
              <th style={th}>Aberto</th>
              <th style={th}>Respondido</th>
              <th style={{ ...th, textAlign: 'right' }}>Nota</th>
            </tr>
          </thead>
          <tbody>
            {rows.map((r, i) => (
              <tr key={`${r.idCliente}-${r.recordDate}-${i}`}>
                <td style={td}>
                  <div>{r.nomeCliente}</div>
                  <div style={idMono}>#{r.idCliente}</div>
                </td>
                <td style={td}>{r.nomeAssessor ?? '—'}</td>
                <td style={{ ...td, fontFamily: 'var(--f-mono)', fontSize: 13 }}>
                  {fDate(r.recordDate)}
                </td>
                <td style={td}>
                  <Badge ok={r.emailOpened === 'Sim'} />
                </td>
                <td style={td}>
                  <Badge ok={r.surveyFinished === 'Sim'} />
                </td>
                <td style={notaCell}>
                  {r.nota === null ? (
                    <span style={{ color: 'var(--fg-faint)' }}>—</span>
                  ) : (
                    fNota(r.nota)
                  )}
                </td>
              </tr>
            ))}
            {rows.length === 0 && (
              <tr>
                <td colSpan={6} style={{ ...td, textAlign: 'center', color: 'var(--fg-faint)' }}>
                  Sem envios de NPS no período.
                </td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      {/* Paginação */}
      {totalPages > 1 && (
        <div
          style={{
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
            marginTop: 12,
            fontFamily: 'var(--f-mono)',
            fontSize: 11,
            color: 'var(--fg-mute)',
          }}
        >
          <span>
            Página {page + 1} de {totalPages} · {envios.length} envios
          </span>
          <div style={{ display: 'flex', gap: 8 }}>
            <button
              onClick={() => setPage((p) => Math.max(0, p - 1))}
              disabled={page === 0}
              style={{
                padding: '6px 12px', borderRadius: 8,
                border: '1px solid var(--line-strong)',
                background: 'var(--bg-elev)',
                color: page === 0 ? 'var(--fg-faint)' : 'var(--fg)',
                fontFamily: 'var(--f-mono)', fontSize: 11,
                cursor: page === 0 ? 'not-allowed' : 'pointer',
              }}
            >
              ← Anterior
            </button>
            <button
              onClick={() => setPage((p) => Math.min(totalPages - 1, p + 1))}
              disabled={page >= totalPages - 1}
              style={{
                padding: '6px 12px', borderRadius: 8,
                border: '1px solid var(--line-strong)',
                background: 'var(--bg-elev)',
                color: page >= totalPages - 1 ? 'var(--fg-faint)' : 'var(--fg)',
                fontFamily: 'var(--f-mono)', fontSize: 11,
                cursor: page >= totalPages - 1 ? 'not-allowed' : 'pointer',
              }}
            >
              Próxima →
            </button>
          </div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Integrar na página `nps/page.tsx`**

Substituir o bloco de placeholder (`{data === null && ...} {data && data.envios.length === 0 && ...} {data && data.envios.length > 0 && ...}`) por:

```tsx
{data === null ? (
  <div style={{ padding: 24, color: 'var(--fg-faint)', fontFamily: 'var(--f-text)' }}>
    Não foi possível carregar dados de NPS.
  </div>
) : (
  <NpsTable envios={data.envios} />
)}
```

E adicionar no topo do arquivo, junto aos outros imports:

```tsx
import { NpsTable } from './_components/NpsTable'
```

- [ ] **Step 3: Validar typecheck**

Run: `npm run typecheck`
Expected: PASS.

- [ ] **Step 4: Validar visualmente**

Run: `cd server && npm run dev` + `npm run dev`, abrir `http://localhost:3000/nps`.

Conferir:
- Tabela renderiza com 100 linhas na página 1 (deve ter 8 páginas para 744 envios).
- Colunas: `Cliente | Assessor | Envio | Aberto | Respondido | Nota`.
- Cada linha de Cliente mostra nome em cima e `#id_cliente` embaixo em mono/cinza.
- Badges "Sim" verde (pos-bg/pos-fg) e "Não" vermelho (neg-bg/neg-fg), filled sem outline.
- Coluna Nota: alinhada à direita, mono; `—` cinza quando null.
- Rodapé mostra "Página 1 de 8 · 744 envios" e botões Anterior/Próxima funcionais.
- Botão Anterior desabilitado na página 1; Próxima desabilitado na 8.

- [ ] **Step 5: Commit**

```bash
git add "src/app/(auth)/nps/_components/NpsTable.tsx" "src/app/(auth)/nps/page.tsx"
git commit -m "feat(nps): tabela de envios com paginação e badges pos/neg

100 linhas/página, badges filled seguindo design system, coluna
Nota em mono alinhada à direita."
```

---

## Task 7: Sidebar + demo mock + validação final por role

Fechar a feature: adicionar item "NPS" na Sidebar (grupo Principal, após Clientes), criar `demoNps` mock e integrar na página, e validar manualmente todos os roles.

**Files:**
- Modify: `src/app/(auth)/_components/Sidebar.tsx:112` (inserir novo item antes de `/relatorios`)
- Modify: `src/lib/mock/demo.ts` (adicionar `demoNps` no final)
- Modify: `src/app/(auth)/nps/page.tsx` (usar `demoNps` quando `session.isDemo`)

**Interfaces:**
- Consumes: `Envio`, `NpsPayload` da página (implícito no shape do mock)
- Produces:
  ```ts
  export const demoNps: {
    kpis: { envios: number; aberturas: number; respostas: number; mediaNota: number | null }
    envios: Envio[]
  }
  ```

- [ ] **Step 1: Adicionar item na Sidebar**

Em `src/app/(auth)/_components/Sidebar.tsx`, localizar o bloco de `/clientes` (linhas 95-111) e o de `/relatorios` (linhas 112-126). Inserir entre eles:

```tsx
      {
        href: '/nps',
        label: 'NPS',
        icon: (
          <svg
            aria-hidden="true"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1.5"
          >
            {/* Balão de chat com estrela dentro */}
            <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z" />
            <polygon points="12 7 13.2 9.5 16 9.9 14 11.9 14.5 14.5 12 13.3 9.5 14.5 10 11.9 8 9.9 10.8 9.5" />
          </svg>
        ),
      },
```

- [ ] **Step 2: Criar `demoNps` em `src/lib/mock/demo.ts`**

No final do arquivo, adicionar:

```ts
// ─── NPS (demo João Paulo — 46 envios, 20 aberturas, 5 respostas) ────────────

type DemoEnvio = {
  idCliente: number
  nomeCliente: string
  emailCliente: string | null
  telefone: string | null
  nomeAssessor: string | null
  recordDate: string
  emailOpened: string | null
  surveyFinished: string | null
  nota: number | null
}

// Gera 46 envios batendo com a base demoClientes. 20 abriram email, 5 responderam com notas 8-10.
const _demoEnvios: DemoEnvio[] = demoClientes.clientes.map((c, i) => {
  const opened = i < 20
  const finished = i < 5
  const nota = finished ? (8 + (i % 3)) : null // notas 8, 9, 10, 8, 9
  return {
    idCliente: c.id_cliente,
    nomeCliente: c.nome_cliente,
    emailCliente: c.email_cliente,
    telefone: c.telefone,
    nomeAssessor: c.nome_assessor,
    recordDate: `2026-08-${String((i % 20) + 1).padStart(2, '0')}T10:00:00.000000`,
    emailOpened: opened ? 'Sim' : 'Não',
    surveyFinished: finished ? 'Sim' : 'Não',
    nota,
  }
})

export const demoNps = {
  kpis: {
    envios: 46,
    aberturas: 20,
    respostas: 5,
    mediaNota: 8.8, // (8+9+10+8+9)/5
  },
  envios: _demoEnvios,
}
```

- [ ] **Step 3: Usar `demoNps` na página**

Em `src/app/(auth)/nps/page.tsx`, adicionar import:

```tsx
import { demoNps } from '@/lib/mock/demo'
```

Modificar a assinatura de `getNps` para receber `isDemo`, e no início retornar mock:

```ts
async function getNps(
  email: string,
  role: string,
  equipe?: string,
  idAssessor?: string,
  isDemo?: boolean,
): Promise<NpsPayload | null> {
  if (isDemo) return demoNps
  try {
    // ...resto igual
  }
}
```

E atualizar a chamada em `NpsPage`:

```ts
const data = await getNps(
  session.email,
  session.role,
  session.equipe,
  session.idAssessor,
  session.isDemo,
)
```

- [ ] **Step 4: Validar typecheck**

Run: `npm run typecheck`
Expected: PASS.

- [ ] **Step 5: Validar Sidebar visualmente**

Run: `cd server && npm run dev` + `npm run dev`. Logar e observar a Sidebar:
- Item "NPS" aparece entre "Clientes" e "Relatórios".
- Ícone renderiza (balão + estrela).
- Clicar no item navega para `/nps` e destaca o item (padrão active state da Sidebar).

- [ ] **Step 6: Validar todos os roles**

Rodar dev servers ainda. Testar:

**a) Como admin/master**: KPIs mostram 744/311/26/9,14 (ou números atuais da base). Tabela tem 744 envios em 8 páginas. Botão CSV baixa ~718 linhas.

**b) Como assessor**: (logar com um email de assessor real ou criar uma session — se não tiver como logar como assessor localmente, testar em preview URL do Cloudflare Pages depois do PR). KPIs devem ser MENORES que admin. Todos os envios devem ser do próprio assessor. CSV baixado só tem os próprios clientes.

**c) Como demo**: logar em `/demo` (padrão do projeto). KPIs mostram 46/20/5/8,80. Tabela tem 46 envios em 1 página (menor que PAGE_SIZE). Botão CSV: o mock não gera CSV real — o proxy chama o Worker que consulta a base real. Aceitar esse comportamento (mock cobre a página, não o endpoint CSV — se quisermos mockar o CSV também, é escopo futuro).

**d) Como usuário sem acesso** (role inválida): página deve mostrar "Não foi possível carregar dados de NPS." (é o fallback quando o Worker retorna 403).

Parar dev servers.

- [ ] **Step 7: Rodar Biome check**

Run: `npm run check`
Expected: PASS (sem warnings novos). Se houver, rodar `npm run check:fix`.

- [ ] **Step 8: Commit final**

```bash
git add "src/app/(auth)/_components/Sidebar.tsx" src/lib/mock/demo.ts "src/app/(auth)/nps/page.tsx"
git commit -m "feat(nps): item na Sidebar + demo mock para persona João Paulo

Fecha feature: link 'NPS' no grupo Principal (entre Clientes e
Relatórios), mock com 46 envios/20 aberturas/5 respostas para
usar em vídeos de apresentação."
```

- [ ] **Step 9: Verificar diff completo antes de abrir PR**

Run: `git log --oneline main..HEAD`
Expected: 7 commits (Tasks 1-7) — todos com scope apropriado (`refactor(api)`, `feat(nps)`).

Run: `git diff main --stat`
Confirmar que apenas os arquivos listados na File Structure foram tocados.
